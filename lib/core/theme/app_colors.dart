import 'package:flutter/material.dart';

class AppColors {
  // Light Theme Colors - Adjusted based on the UI in the image
  static const primary = Color(0xFF1A8E35); // Darker green from the image
  static const secondary = Color(0xFF4CAF50); // Original primary as secondary
  static const accent =
      Color(0xFFFFB74D); // More orange-ish for weather icon and AI advice
  static const backgroundLight = Color(0xFFF1F8E9);
  static const textLight = Color(0xFF212121);
  static const textLightSecondary = Color(0xFF757575);

  // Dark Theme Colors
  static const backgroundDark = Color(0xFF263238);
  static const textDark = Color(0xFFE0E0E0);
  static const textDarkSecondary = Color(0xFFB0BEC5);

  // Common Colors
  static const error = Color(0xFFD32F2F);
  static const surface = Colors.white;
  static const surfaceDark = Color(0xFF37474F);
}
